import sortableHeader from './sortable-header.component';
import './sortable-header.scss';

export default angular
  .module('common.sortable-header', [])
  .component('rlSortableHeader', sortableHeader)
  .name;
